# This is a test file yes it is
# no it isn't
git remote add origin https://github.com/stainesa/git_test
git config remote.origin.url git@github.com:stainesa/git_test
git pull -u origin master
git push -u origin master

